"""
Пакет 'base' содержит фундаментальные строительные блоки системы.

Здесь определены базовые абстракции, исключения и корневые сущности, 
от которых зависит весь остальной код. Этот пакет не имеет зависимостей 
от других пакетов проекта (кроме стандартных библиотек), что предотвращает 
циклические импорты.

Состав пакета:
- Employee (employee.py):
  Корневой класс данных. Реализует инкапсуляцию, валидацию базовых полей 
  (id, имя, отдел, оклад) через свойства. Не содержит бизнес-логики.

- AbstractEmployee (abstract_employee.py):
  Абстрактный интерфейс (Interface Layer). Наследуется от Employee и добавляет:
  1. Абстрактные методы (контракт): calculate_salary, get_info, to_dict.
  2. Магические методы: сравнение (__eq__, __lt__), арифметика (__add__, __radd__).

- exceptions.py:
  Набор кастомных исключений для обработки ошибок бизнес-логики:
  AppError, EmployeeNotFoundError, DependencyError, InvalidStatusError и др.
"""
